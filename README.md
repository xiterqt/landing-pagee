# Target Exclusive Discount

A lightweight recreation of the supplied mobile landing page.

Open `index.html` directly in a browser. The Start Now button links to the configured offer destination.
